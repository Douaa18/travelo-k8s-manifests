apiVersion: v1
kind: Secret
metadata:
  name: travelo-secret
  namespace: {{ .Values.namespace }}
type: Opaque
data:
  MYSQL_ROOT_PASSWORD: {{ .Values.mysql.rootPassword | b64enc }}
  SPRING_DATASOURCE_USERNAME: {{ .Values.mysql.rootPassword | b64enc }}
  SPRING_DATASOURCE_PASSWORD: {{ .Values.mysql.rootPassword | b64enc }}