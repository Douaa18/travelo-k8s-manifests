apiVersion: v1
kind: ConfigMap
metadata:
  name: travelo-config
  namespace: {{ .Values.namespace }}
data:
  SPRING_DATASOURCE_URL: "jdbc:mysql://travelo-mysql:3306/{{ .Values.mysql.database }}"
  MYSQL_DATABASE: "{{ .Values.mysql.database }}"